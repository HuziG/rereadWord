<!-- components/auth/index.wxml -->
<view class="container">
  <van-button round bind:click="getPhoneNumber" type="warning" loading="{{loading}}" disabled="{{loading}}">
    <text wx:if="{{loading===false}}">立即使用</text>
  </van-button>
  <view class="des">*我们不要您的任何隐私数据，您可以直接使用</view>
</view>