<!--components/study/radio/index.wxml-->
<view class="btn-wrapper voice-wrapper" bind:tap="playHandle">

  <view wx:if="{{play}}">
    <view class="iconfont icon-pause"></view>   
  </view>

  <view wx:if="{{!play}}">
    <view class="iconfont icon-caret-right"></view>  
  </view>

</view>
