<!-- components/mode/index.wxml -->
<view class="container">
  <block wx:for="{{modeArr}}" wx:key="*this">
    <view class="mode-item dark-text-color" data-mode="{{item.key}}" bind:tap="choiceMode">
      {{item.name}}
    </view>
  </block>
</view>